package pre_processor;

public class PreProcessor {
    void execute(String instruction) {
        switch (instruction) {
            case "import":
                break;
            case "replace":
                break;
            case "from %d to %d":
        }
    }
}